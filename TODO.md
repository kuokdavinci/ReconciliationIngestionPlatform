## UI 

### review packet
preview sample step 3 cần gọn lại 
internal db không ghi nhận đúng thông tin khi mở review packet của backfill scenario 
### Schedule run status
kẹt 2 state running và watting review 
chọn option action chưa tối ưu 
không cập nhật state của các run trong schedule khi chuyển state.
### Runtime detail 
event timeline chưa trực quan, cần visualize, chữ đỏ không phù hợp(chỉ dùng khi biểu diễn lỗi)

## flow
chưa có luồng chạy backfill trong review packet (luồng chưa phù hợp) 