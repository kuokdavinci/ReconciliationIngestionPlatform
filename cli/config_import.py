"""Configuration import CLI handler (Excel template parsing).

Config import is bundled with ingestion in cli/ingest.py:
  - parse_excel_template() handles Excel template parsing.
  - run_ingestion() handles --config flag (upload config then run pipeline).

This module is reserved for future standalone config import features.
"""
