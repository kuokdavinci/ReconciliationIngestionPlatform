# Sprint 3 — Workstream A EDA review: Fraud Detection Dataset

## Closeout scope

Workstream A is complete for EDA discovery, the reproducible profile,
provenance, the frozen ingestion baseline, controlled mutations, and the
coverage handoff. It is not production approval: operator ownership,
observability and production acceptance remain outside A.

The Kaggle notebook is unchanged and Kaggle-only. The suggestions below are
not instructions to edit or run it locally. Workstream B and the deterministic
parts of Workstream C are now implemented; Workstreams D–F remain handoffs.

## Provenance and full-dataset finding

- Dataset: **Fraud Detection Dataset**
- Profile version: `3`
- Dataset SHA-256:
  `e3895c988fe37efc76dabfe62d23f7ab75e89477bb17ba0c53092b008431caf6`
- Profile result: `valid=1,000,000`, `rejected=0`, `duplicate=0`
- Decision: `PASS` for deterministic ingestion-quality checks
- Amount IQR: descriptive observation only; it is not a quality-gate rule
- Focused profile coverage: `26` tests passing, including controlled mutation
  cases.

This is evidence about this file and profile contract, not a production
partner/business contract. The clean source data does not itself prove reject,
quarantine, or conflict handling.

## Profile boundary

The executable profile is now generic and lives in
`scripts/eda/quality_profile.py`. The current 17-column source schema is only
an adapter configuration in `scripts/eda/fraud_detection_dataset.py`, and the
artifact is generated by `scripts/eda/profile_fraud_dataset.py`.

Fraud-label conditionality, coordinate ranges, card/customer relationships, and
merchant/location relationships remain EDA findings from the Kaggle notebook.
They are not ingestion-quality rules because this platform is not a fraud
detection service and the current canonical mapping does not require those
semantics.

## Source-to-canonical contract

The source has these 17 columns, in benchmark order:

`transaction_id`, `timestamp`, `customer_id`, `card_id`, `device_id`,
`ip_address`, `merchant_id`, `merchant_category`, `merchant_country`,
`merchant_city`, `merchant_latitude`, `merchant_longitude`,
`transaction_type`, `amount`, `currency`, `is_fraud`, `fraud_type`.

The historical Workstream A frozen baseline mapped them as follows. The current
Workstream C benchmark v2 promotes `timestamp` to required canonical
`transDate`; the Workstream C evidence document is authoritative for runtime
behavior.

| Source field | Mapping reference | Contract note |
|---|---|---|
| `transaction_id` | `id` | Required. |
| `timestamp` | `transDate` | Required `DATE`; ISO/offset values normalize to UTC-aware canonical timestamps under Workstream C. |
| `customer_id` | `extra.customerId` | Context only. |
| `card_id` | `extra.cardId` | Context only. |
| `device_id` | `extra.deviceId` | Context only. |
| `ip_address` | `extra.ipAddress` | Context only. |
| `merchant_id` | `extra.merchantId` | Context only. |
| `merchant_category` | `extra.merchantCategory` | Context only. |
| `merchant_country` | `extra.merchantCountry` | Context only. |
| `merchant_city` | `extra.merchantCity` | Context only. |
| `merchant_latitude` | `extra.merchantLatitude` | Context only. |
| `merchant_longitude` | `extra.merchantLongitude` | Context only. |
| `transaction_type` | `extra.transactionType` | Context only. |
| `amount` | `amount` | Required `DECIMAL`. |
| `currency` | `currency` | Required `STRING`. |
| `is_fraud` | `extra.isFraud` | Source label retained as context. |
| `fraud_type` | Not mapped | Intentionally absent from the benchmark and not canonical. |
| — | `status ← SUCCESS` | Benchmark constant, not a source column. |

The EDA claim that `transaction_id` is unique is **within this file only**.
Persistence idempotency has a different scope: PostgreSQL treats
`(identify, ingestion_key)` as the unique key. Neither result substitutes for
the other.

## EDA evidence and rule candidates

| Candidate / rule | EDA evidence | Level | Proposed outcome / action | Contract need | False-positive risk |
|---|---|---|---|---|---|
| Required schema and fields | Stable 17-column shape; required profile fields present | Deterministic | Reject missing required mapped values; fail incompatible file shape | Mapping/schema contract | Low once agreed |
| Timestamp format, timezone, precision | ISO timestamps carry timezone and second precision | Deterministic parse; precision monitoring | Do not promote until `transDate` parsing/normalization is defined | Timestamp format, timezone, precision | High before agreement |
| Amount parse and negative amount | Decimal profile; no source rejection | Deterministic | Reject unparseable or negative values; retain zero as valid | Monetary semantics | Low |
| Amount outlier | Right-skewed distribution; IQR candidate | Statistical | Observation only; never auto-reject or change the profile decision | Approved threshold | High |
| Equivalent duplicate | No file-local duplicate observed | Idempotency | Count/skip at persistence boundary | Identity and replay scope | Medium |
| Conflicting duplicate | No natural case proves payload comparison | Integrity candidate | Quarantine/review only after payload comparison contract | Identity plus payload equality | High |
| Fraud conditional/domain | `fraud_type` is conditionally meaningful with fraud label | Business semantic | Monitoring only; do not make canonical or reject | Fraud-label domain/conditional semantics | High |
| Coordinates | Location fields and range are profiled | Semantic | Monitoring/review only | Coordinate interpretation and CRS/range policy | High |
| Card/customer consistency | Entity relationships can be inconsistent | Semantic | Monitoring/review only | Card-to-customer identity model | High |
| Merchant/location consistency | Merchant-to-location relationships can be inconsistent | Semantic | Monitoring/review only | Merchant/location ownership model | High |
| Temporal volume | Time distribution is descriptive baseline evidence | Operational | Monitoring/review only | Expected calendar and volume baseline | High |

## EDA-to-runtime coverage handoff

| Candidate | Status | Current boundary | Handoff owner |
|---|---|---|---|
| Required ID, amount, currency, and Decimal conversion | COVERED | Mapping marks the fields required; normalizer converts `DECIMAL`; validator enforces canonical required fields | A closed; maintain in B/C |
| Negative amount | COVERED | Validator rejects negative values; zero remains valid | A closed; maintain in C |
| ISO timezone timestamp to `transDate` | COVERED | Workstream C maps the required field, normalizes ISO/offset values to UTC, and proves normal/fast parity | C; D operates routed rejects |
| Equivalent duplicate / idempotency | PARTIAL | PostgreSQL handles `(identify, ingestion_key)`; EDA proves only file-local uniqueness | B |
| Conflicting duplicate | PARTIAL | Quarantine persistence exists, but `ON CONFLICT DO NOTHING` does not compare payloads | B/D |
| Header/schema drift | PARTIAL | `StructureSignature`, ConfigHealth, and mapping coverage detect structure signals; a type-aware runtime gate is still absent | B/C |
| Amount IQR/outlier | DO_NOT_PROMOTE | Current profile evidence only; warning/review candidate | Partner/business contract |
| Fraud semantics | DO_NOT_PROMOTE | Source label is retained in `extra`; no canonical rule | Partner/business contract |
| Card/customer consistency | DO_NOT_PROMOTE | Observational EDA relationship only | Partner/business contract |
| Merchant/location consistency | DO_NOT_PROMOTE | Observational EDA relationship only | Partner/business contract |
| Coordinate semantics | DO_NOT_PROMOTE | Observational coordinate evidence only | Partner/business contract |
| Temporal volume | DO_NOT_PROMOTE | Descriptive profile baseline only | Partner/business contract |

## Notebook guidance (suggestions only)

If the Kaggle-only notebook is revisited, it may display provenance, separate
expected from unexpected conditional nulls, show timezone/precision effects,
and distinguish file-local uniqueness from persistence identity. It may also
link controlled mutation results. Those are suggestions only; the notebook is
unchanged.
