{
  "name": "adapter-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Legacy frontend disabled. Use: npm run dev from repo root or cd frontend-next && npm run dev' && exit 1",
    "build": "echo 'Legacy frontend disabled. Use: npm run build from repo root or cd frontend-next && npm run build' && exit 1",
    "preview": "echo 'Legacy frontend disabled. Run the Next.js app from frontend-next instead.' && exit 1",
    "legacy:dev": "vite",
    "legacy:build": "vite build",
    "legacy:preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
