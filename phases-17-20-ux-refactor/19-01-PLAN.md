---
phase: 19
plan: 01
subsystem: frontend
tags: [review-center, approve, reject, validate, send-to-studio]
dependencies: []
wave: 1
autonomous: true
must_haves:
  - "Review Center" replaces "Review Queue" in all visible UI copy
  - Validate, approve-activate, approve-keep, reject, send-to-studio all work on pending items
key-files:
  created: []
  modified:
    - frontend/app.js
    - frontend/styles.css
---

# Plan 19-01: Review Center — Full Workflow

## Goal

The Review Center UI already exists with `renderApprovals()` showing pending reviews, approve/keep/reject/send-to-studio actions, and validation. This plan completes remaining rename touchpoints and ensures all 5 actions work reliably.

## Tasks

### Task 1: Rename remaining "Review Queue" → "Review Center"

Replace user-facing references:
- `target.type === "review_queue"` (line 2664) — no UI impact, internal key only
- `action === "go-review-queue"` (line 2744) — backward-compat alias, OK to keep
- Verify page subtitle, titles, toasts all say "Review Center"

### Task 2: Verify all 5 review actions work

Test in `bindViewActions`:
- `validate-runtime-packet` (line 2961) — calls `POST /api/v1/review-packets/{id}/validate-runtime`
- `approve-packet-activate` — calls `approve-activate` endpoint
- `approve-packet-keep-current` — calls `approve-keep-current`
- `reject-packet` — calls `reject`
- `send-packet-to-studio` — navigates to `#mapping-studio` with IDs

These endpoints already exist in `src/api/review_packets.py`. Verify they all map correctly and the frontend handles responses.

### Task 3: Scope override selector

The backend `ReviewDecisionPayload` accepts `scopeType` field. The frontend should pass it from a scope selector UI element when approving. This is already partially wired in `renderApprovals`.
