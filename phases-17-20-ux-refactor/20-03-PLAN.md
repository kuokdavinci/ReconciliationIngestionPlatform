---
phase: 20
plan: 03
subsystem: backend + frontend
tags: [auto-trigger, reconciliation, approve]
dependencies: []
wave: 2
autonomous: true
must_haves:
  - Approve & activate triggers reconciliation run
  - Toast shows confirmation with result count
  - No double-reconciliation on same partner+date
key-files:
  created: []
  modified:
    - src/scheduler/reconciliation.py
    - frontend/app.js
---

# Plan 20-03: Auto-trigger Reconciliation After Approval

## Goal

`postApproveRun` already exists in the backend and frontend. This plan verifies and completes the auto-trigger flow.

## Tasks

### Task 1: Verify postApproveRun backend

Check `src/scheduler/reconciliation.py` for the reconciliation trigger function. Verify it's called in the approve-activate handler in `src/api/review_packets.py` (line 416).

### Task 2: Verify frontend toast

The frontend already shows reconciliation results after approve-activate (line 2939-2948). Verify the toast text is complete and the reconciliation page renders fresh data after the action.
